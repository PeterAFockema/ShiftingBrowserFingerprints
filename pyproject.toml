[build_system]
requires = ["setup_tools"]
build-backend = "setup_tools.build_meta"

[project]
name = "shifting_browser_fingerprints"
version = "0.1.02"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"*" = ["*.*"]
